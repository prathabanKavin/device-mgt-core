name: PR Build Check

on:
  pull_request:
    branches:
      - srcbranch

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout Code
        uses: actions/checkout@v4

      - name: Run Maven Build in Docker
        uses: addnab/docker-run-action@v3
        with:
          image: maven:3.9-eclipse-temurin-11
          options: --rm
          workdir: /
          volumes: |
            ${{ github.workspace }}:/
          run: |
            echo "Contents of / inside the container:"
            ls -al /
            mvn clean install

